<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spm.sejarah">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".notabab1" />
        <activity android:name=".login_teacher" />
        <activity android:name=".login_student" />
        <activity android:name=".login_admin" />
        <activity android:name=".listkelas" />
        <activity android:name=".cartajuara" />
        <activity android:name=".notaform4" />
        <activity android:name=".menupageadmin" />
        <activity android:name=".menupageteacher" />
        <activity android:name=".menupage" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>